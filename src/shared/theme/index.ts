import {createTheme} from "@mui/material";

export const myPortfolioTheme = createTheme({
    typography: {
        'fontFamily': `'Montserrat', 'sans-serif'`
    },
    palette:{
        primary: {
            main: '#A60000'
        }
    }

})
